{
  "name": "ai-shopping-recommendation",
  "version": "1.0.0",
  "description": "AI-powered shopping recommendation system for digital products",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && python -m uvicorn main:app --reload",
    "build": "npm run build:frontend",
    "build:frontend": "cd frontend && npm run build",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && pip install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": ["ai", "shopping", "recommendation", "react", "fastapi"],
  "author": "AI Shopping Team",
  "license": "MIT"
}